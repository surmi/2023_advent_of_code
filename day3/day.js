function run1star(inPath){
    console.log('Day 3*');

    let lines;
    try {
        lines.require('fs').readFileSync(inPath, 'utf-8').split(\n);
    }
    catch (err){
        console.log(err);
    }

    
}